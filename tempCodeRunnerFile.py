        # # Main control buttons
        # self.reset_button = pygame.Rect(margin, y_base, button_width, button_height)
        # self.undo_button = pygame.Rect(margin * 2 + button_width, y_base, button_width, button_height)
        # self.prev_button = pygame.Rect(margin * 3 + button_width * 2, y_base, button_width, button_height)
        # self.next_button = pygame.Rect(margin * 4 + button_width * 3, y_base, button_width, button_height)

        # # Solver buttons
        # self.bfs_button = pygame.Rect(margin, y_solver, button_width, button_height)
        # self.dfs_button = pygame.Rect(margin * 2 + button_width, y_solver, button_width, button_height)
        # self.astar_button = pygame.Rect(margin * 3 + button_width * 2, y_solver, button_width, button_height)
        # self.stop_button = pygame.Rect(margin * 4 + button_width * 3, y_solver, button_width, button_height)
